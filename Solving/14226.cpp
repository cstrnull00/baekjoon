#include <iostream>
#include <utility>

using namespace std;

int main() {
    int goal, cnt = 0, screen = 1, clipboard = 0;
    pair<int, int> emoticon = make_pair(screen, clipboard);

    cin >> goal;

    while (screen < goal) {
        
    }
    
    cout << cnt;

    return 0;
}